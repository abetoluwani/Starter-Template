import 'package:get/get.dart';

class RootBinding extends Bindings {
  @override
  void dependencies() {
    // Example of other app-level services:
    // Get.put(ApiService());
    // Get.put(StorageService());
    // Get.put(NotificationService());
  }
}
